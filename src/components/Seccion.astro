---
interface Props {
  id: string;
  tituloHtml: string;
}

const { id, tituloHtml } = Astro.props;
---

<section class="card" id={id} aria-labelledby={`${id}-h`}>
  <h2 id={`${id}-h`} set:html={tituloHtml} />
  <slot />
</section>
